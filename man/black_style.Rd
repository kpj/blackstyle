% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{black_style}
\alias{black_style}
\alias{use_black_style}
\alias{black_style_transformer}
\alias{black_style_text}
\alias{black_style_file}
\alias{black_style_dir}
\alias{black_style_pkg}
\title{The blackstyle Code Style}
\usage{
use_black_style()

black_style_transformer(...)

black_style_text(text, ...)

black_style_file(path, ...)

black_style_dir(path, ...)

black_style_pkg(pkg = ".", ...)
}
\arguments{
\item{...}{Arguments passed to underling \pkg{styler} functions (identified
by removing the \code{black_} prefix), except for \code{transformers}, which is set to
the \code{black_style_transformer()} internally.}

\item{text}{A character vector with text to style.}

\item{path}{A character vector with paths to files to style.}

\item{pkg}{Path to a (subdirectory of an) R package.}
}
\description{
Use \code{\link[styler:style_text]{styler::style_text()}} to format code according to the unofficial
\pkg{blackstyle} style guide. Follows the
\href{https://style.tidyverse.org}{tidyverse style guide} as implemented by
\code{\link[styler:tidyverse_style]{styler::tidyverse_style()}}, but with additional style rules that enforce
consistent line breaks inside function calls and left-justification of
function arguments in function definitions.
}
\section{Functions}{
\itemize{
\item \code{use_black_style}: Set the \pkg{blackstyle} style as the default style for
\pkg{styler} addins.

\item \code{black_style_transformer}: A code transformer for use with \code{\link[styler:style_text]{styler::style_text()}}

\item \code{black_style_text}: Style text using the \pkg{blackstyle} code style

\item \code{black_style_file}: Style a file using the \pkg{blackstyle} code style

\item \code{black_style_dir}: Style a directory using the \pkg{blackstyle} code style

\item \code{black_style_pkg}: Style a package using the \pkg{blackstyle} code style
}}

\section{Using the \pkg{blackstyle} code style}{
 You can set the \pkg{blackstyle}
code style as the default code style for \pkg{styler} (and its associated
RStudio addins, like "Style active file" and "Style selection") by calling
\code{blackstyle::use_black_style()}. If you would rather set this option globally
for all session, you can add the following to your \code{.Rprofile}:\preformatted{options(styler.addins_style_transformer = "blackstyle::black_style_transformer()")
}
}

\examples{
\dontrun{
use_black_style()
# Use styler addins
styler:::style_selection()
}

ex_code <- "mtcars \%>\% mutate(mpg = mpg * 2,\n\t\t     cyl = paste(cyl)) \%>\% head()"
cat(ex_code)

black_style_text(ex_code)
}
